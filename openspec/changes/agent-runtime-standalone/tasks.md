## 1. 前置与打包可行性验证

- [ ] 1.1 确认 `agent-runtime-hosted-ports` 第 2–3 组任务（会话接口、交互挂起请求）已完成
- [ ] 1.2 【验证 R1】在干净的 Python 3.11 虚拟环境中构建上游整包 wheel 并安装，只用 wheel（不含仓库检出）运行 `import run_agent` 与一轮假模型对话，记录结论；若失败，评估改为 venv 目录分发或提前启用路线 A
- [ ] 1.3 记录冷启动基准：导入耗时、加载模块数、常驻内存，作为路线切换的依据
- [ ] 1.4 把 explore 阶段 spike 的隔离运行器、访问记录器、路径感知假模型端点迁入 `agent_runtime/tests/standalone/`

## 2. Standalone 初始化与状态隔离（路线 C）

- [ ] 2.1 `bootstrap(mode="standalone", hermes_home=...)`：在导入任何上游模块前设置 `HERMES_HOME`；若上游模块已导入，报初始化顺序错误
- [ ] 2.2 进程级约束：以不同状态目录重复初始化时报初始化冲突错误
- [ ] 2.3 配置生成：由 `RuntimeSpec` 与宿主 ConfigPort 合成实例目录下的 `config.yaml`，其中 `plugins.enabled` 为空；凭证只经凭证接口写入
- [ ] 2.4 打包排除安装目录内的 `.env`；测试确认上游 `project_env` 路径不存在该文件
- [ ] 2.5 内置 `YamlConfig`、`DictConfig`、`EnvCredentials`（显式密钥 + 指定环境变量名）

## 3. 能力门控与状态库

- [ ] 3.1 定义 Standalone 能力表：定时任务、消息投递、会话检索、OAuth 类 provider、第三方插件
- [ ] 3.2 开会话时按能力表自动禁用工具集；显式请求不可用工具集时，在模型调用之前报能力不可用
- [ ] 3.3 定位构造阶段创建 `state.db` 的调用方；审计 `process_registry_results`、`async_delegation`、`bot_live_delivery` 等直接打开状态库的路径，并按工具集门控
- [ ] 3.4 测试：未提供会话存储时，完成三轮对话后会话数据库中没有这三轮的消息

## 4. 端点探测与审批

- [ ] 4.1 验证预置 `model.context_length` 能否抑制首轮前的全部探测请求；不能抑制的，按来源增加配置开关，无法关闭的列入构建报告
- [ ] 4.2 测试：预置上下文长度后，假模型端点只收到对话接口请求
- [ ] 4.3 接入 hosted-ports 的审批上下文与挂起请求；测试 Standalone 下无交互实现时危险命令不执行、批准后执行

## 5. 端到端与隔离验收

- [ ] 5.1 E2E：纯对话、`read_file`、终端 `echo`、记忆写入四个轮次在干净环境中全部完成
- [ ] 5.2 写入范围审计：测试中把 `~/.hermes` 替换为只读哨兵目录，任何访问即失败；实例目录外无新增文件
- [ ] 5.3 用户目录配置不生效：`~/.hermes/config.yaml` 与 `.env` 中放入其他模型和密钥，确认请求只发往宿主下发的端点
- [ ] 5.4 第三方插件不加载：安装一个声明 entry point 的测试插件，确认其钩子和中间件未被调用
- [ ] 5.5 在上游兼容层 revert 之后的新基线上重跑 5.1–5.4

## 6. 打包与发布（路线 C）

- [ ] 6.1 构建脚本：产出包含上游整包与 `hermes_agent_runtime` 的产物，版本号为 `<运行时版本>+hermes.<上游版本>`
- [ ] 6.2 可重复构建：同一基线两次构建，产物内所有文件的哈希一致
- [ ] 6.3 构建报告：记录路线、不可用工具集与能力清单
- [ ] 6.4 干净环境安装验收流水线：只安装产物，运行 Standalone E2E

## 7. 路线 A 后备（仅在确认「产物不得包含外层代码」时实施）

- [ ] 7.1 记录切换决定与依据（体积、合规或攻击面要求），更新 design D1
- [ ] 7.2 构建选项启用抽取路线；按闭包锁原样复制上游文件，漂移时构建失败
- [ ] 7.3 按 T1 / T2 / T3 分层生成替身：T1 原样随行；T2 手写语义函数；T3 访问任何符号即报能力不可用，禁止自动占位
- [ ] 7.4 为每个 T2 函数编写契约输入集，与上游原函数比对，不一致时构建失败
- [ ] 7.5 以运行期导入审计校准替身清单，覆盖 explore 阶段发现的 `acp_adapter.edit_approval`、`gateway.hosted_room_execution_policy`
- [ ] 7.6 抽取产物跑第 5 组全部 E2E，外加「工具调用链经过未实现替身时返回明确错误而非空结果」测试
